public class Z10 {
    void methodOfClassA() {
        // System.out.println(i); can't use a local field before it is defined
        // i = 10; can't initialize a local field before it is defined,In the case of
        // local variables, neither you can use it nor you can initialize it before it
        // is defined.

        int i;
    }
}
